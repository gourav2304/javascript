// Math.random()

// Generate a decimal number lie between 0 to 0.99
Math.random()
 //save it to in creating variable
var decimal = Math.random();
// log the value of decimal to the console
console.log(decimal);
// log the value of decimal MULTIPLIED by 10 
console.log(10 * decimal);
